import styled from "styled-components";

export const Container = styled.footer`
  display: flex;
  flex-direction: row;
  align-items: center;
  margin: 0 auto;
  padding-bottom: 10px;

  > span {
    color: var(--white);
  }
`;
