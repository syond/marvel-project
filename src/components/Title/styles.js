import styled from "styled-components";

export const Container = styled.legend`
  display: flex;
  flex-direction: column;
  align-items: center;
  margin: 0 auto;
  margin-top: 48px;

  > p {
    padding-top: 12px;
  }
`;
